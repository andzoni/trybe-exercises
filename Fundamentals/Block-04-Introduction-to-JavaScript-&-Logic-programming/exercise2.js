/* Make a program that returns the greater of two numbers. At the beginning of the program, define two variables with the values to be compared. */

let a = 20;
let b = 21;

if (a > b) {
  console.log("'a' is bigger than 'b'");
} else {
  console.log("'b' is bigger than 'a'");
};